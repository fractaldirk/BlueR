<p id="notice"></p>
  <p>Greennet: <%= @post.tag %></p>

  <% unless @post.comments.empty? %>
    <h2><%= pluralize(@post.comments.size, 'announcement') %> for <%= @post.idea_title %></h2>
  <hr />
  <div id="comments">
    <%= render @post.comments.order("comments.created_at desc") %>
  </div>

  <% end %>

  <h6>Create an announcement:</h6>
  <%= render "comments/form" %>

  <%= link_to 'Back', posts_path, :id => 'back_button', :class => 'btn' %>
