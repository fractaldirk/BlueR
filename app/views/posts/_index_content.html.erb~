<h1>Imagine</h1>
<br />
  <%= link_to 'New project', new_post_path %>
<br />
<br />

<div>
<table class="table" id="knowledge_table">
  <tr>
    <th>Projects..</th>
    <th>Project leaders..</th>
    <th>Created at</th>
    <th>Action:</th>
  </tr>

<% @posts.each do |post| %>

  <tr>
    <td><%= link_to post.idea_title, post %> (<%= post.comments.count %>)</td>
    <td><%= post.idea_name %></td>
    <td><%= post.created_at.strftime("%d %B %Y") %></td>
    <td><%= link_to 'Delete', post, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
</div>
<% end %>

</table>
