<p id="notice"></p>

<div class="span1">
</div>

<div class="span10">

    <h1 style="color: black"><%= @post.idea_title %> (<%= @post.comments.count %>)</h1>
    <h5>(<%= @post.idea_name %>)</h5>

  <hr />

  <%= render 'projects/participants' %>


  <% unless Project.order(:post_id).where("post_id = post.id").map(&:name).include? current_user.user_name.to_s %>
    <%= render 'projects/form' %>
  <% end %>


  <p>
    <%= simple_format @post.idea_body %>
  </p>
  <hr />

  <% unless @post.comments.empty? %>
    <h2><%= pluralize(@post.comments.size, 'comment') %></h2>
  <hr />
  <div id="comments">
    <%= render @post.comments %>
  </div>

  <% end %>

  <h6>Add a comment:</h6>
  <%= render "comments/form" %>

  <% unless manage_projects == false %>
    <%= link_to 'Edit', edit_post_path(@post), :class => "btn btn-danger", :id => "edit_project_button" %>
  <% end %>

  <%= link_to 'Back', posts_path, :id => 'back_button', :class => 'btn' %>
</div>

<div class="span1">
</div>
