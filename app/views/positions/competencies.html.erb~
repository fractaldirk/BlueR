<p id="notice"></p>

<table>
 
      <tr>
        <th>Competency</th>
        <th>Competency Importance</th>
        <th>Requested Profiency Level</th>
      </tr>
   
   
      <tr>
        <td><% for competency in @position.competencies %>
            <p><%=h competency.competencies %></p>
            <% end %></td>
        <td><% for competency in @position.competencies %>
            <p><%=h competency.competency_cpv %></p>
            <% end %></td>
        <td><% for competency in @position.competencies %>
            <p><%=h competency.competency_value %></p>
            <% end %></td>
      </tr>
   
</table>


<br />
<%= link_to 'Edit', edit_position_path(@position) %> |
<%= link_to 'Back', @position %>
